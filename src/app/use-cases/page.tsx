import {SitePage,PageSection} from '@/components/wikshi-pages/SitePage';
import {WorkflowBrief} from '@/components/wikshi-pages/WorkflowBrief';
import {missions} from '@/components/wikshi-pages/missions';
export const metadata={title:'Use cases | Wikshi'};
export default function UseCases(){return <SitePage eyebrow="Use cases" title="Give it a real mission." accent="Get something useful back." lead="Find the buying signal. Verify the quote. Hear the customer’s story. Specific missions for agents that can research, email, call, and meet people." art="https://wikshi-assets.vercel.app/art/workflow-planner-cutout.10693c46e5bf.webp"><nav className="page-index" aria-label="Mission library">{missions.map(m=><a key={m.id} href={'#'+m.id}>{m.id==='research'?'Buyers':m.id==='interviews'?'Customers':m.id.charAt(0).toUpperCase()+m.id.slice(1)} ↘</a>)}</nav>{missions.map(m=><PageSection key={m.id} id={m.id} label={m.label} title={m.title}><p className="section-intro">{m.body}</p><WorkflowBrief brief={m.brief} preview={m.preview}/>{m.source&&<a className="mission-source" href={m.source} target="_blank" rel="noreferrer">{m.sourceLabel} ↗</a>}</PageSection>)}</SitePage>}
